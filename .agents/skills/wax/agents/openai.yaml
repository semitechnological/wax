interface:
  display_name: "Wax"
  short_description: "Repo-specific patterns and workflows for wax"
  default_prompt: "Use the wax repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true